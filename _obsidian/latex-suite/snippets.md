# LaTeX Suite snippets — load this file once (Settings → "Load snippets from file or folder")

# Curated shorthand for this vault's plain-LaTeX convention.
# Persisted notes always carry the expanded form; these snippets only speed up typing.

ph → physics                                    # word helper
eps → \varepsilon                               # the vault's epsilon (matches notes)
om → \omega                                     # angular frequency
v0 → v_{0}                                      # subscript zero
t1 → t_{1}                                      # subscript one
xrm → x_{\text{rms}}                            # rms value
vfr → v_{\text{rms}}                            # rms speed
fl → \frac{                                        # open a fraction
2f → \frac{1}{2}                                # the half
sq → \sqrt{}                                    # square root
dq → \Delta Q                                   # heat difference
dU → \Delta U                                   # internal-energy difference
vec → \vec{}                                    # vector arrow
[!ab] → > [!abstract]                           # abstract callout
[!tip] → > [!tip]                               # figure/tip callout
